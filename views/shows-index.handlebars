<!-- shows-index.handlebars -->
<h1>Reviews</h1>
</br>
</br>
<div class="row justify-content-between">
<span class = "shows">
<!-- Whew lad, a whole lot of div shenanigans to make things work -->
<div class="d-flex align-self-stretch">

{{#each shows}}
<div class="col-sm-4">
    <!-- I needed seperate div classes for these buttons to float
    properly. Inline didn't work as intended, so I decided on this-->
    <form method="POST" action="/shows/{{this._id}}?_method=DELETE">
    <div class="buttons">
    <button type="submit" class="btn btn-default">Delete</button>
    </div>
    </form>

    <div class="buttons">
    <button onclick="location.href='/shows/{{this._id}}/edit'" class="btn btn-default">
     Edit</button>
    </div>
    <!-- The shows themselves! Wanted to put the shows on the
    bottom right for ease of seeing; had to make that float right -->
    <div class="showindex">
        <a href="/shows/{{this._id}}">
        <h3>{{this.title}}</h3></a>
        <small>{{this.showTitle}}</small>
        <p>{{this.description}}</p>
        </br>
        </br>
    </div>
  <hr>
</div>

{{/each}}
</div>
</div>
</span>
